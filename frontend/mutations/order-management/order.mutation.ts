import { useMutation, useQueryClient } from '@tanstack/react-query';
import { toast } from 'sonner';
import { queryKeys } from '@/lib/common/queryKeys';
import { orderService } from '@/services/order-management';
import type { ConvertCreditPayload, CreditAccountPayload, CreditSettlementPayload, OrderPayload, PackageOrderPayload, PackageOrderSchedulePayload, PackagePayload, PaymentPayload } from '@/types/order-management';
function msg(e: unknown, f: string) { return e instanceof Error ? e.message : f; }
function invalidate(qc: ReturnType<typeof useQueryClient>) { qc.invalidateQueries({ queryKey: queryKeys.orders.root() }); qc.invalidateQueries({ queryKey: queryKeys.credit.root() }); qc.invalidateQueries({ queryKey: queryKeys.catering.root() }); }
export function useCreateOrderMutation(scope: 'waiter'|'cashier'|'public'|'admin' = 'waiter', done?: () => void) { const qc = useQueryClient(); return useMutation({ mutationFn: (payload: OrderPayload) => orderService.createOrder(payload, scope), onSuccess: r => { toast.success(r.message ?? 'Order created'); invalidate(qc); done?.(); }, onError: e => toast.error(msg(e, 'Failed to create order')) }); }
export function useConfirmOrderMutation() { const qc = useQueryClient(); return useMutation({ mutationFn: (id: string|number) => orderService.confirmOrder(id), onSuccess: r => { toast.success(r.message ?? 'Order confirmed'); invalidate(qc); }, onError: e => toast.error(msg(e, 'Failed to confirm order')) }); }
export function useServeOrderMutation() { const qc = useQueryClient(); return useMutation({ mutationFn: (id: string|number) => orderService.serveOrder(id), onSuccess: r => { toast.success(r.message ?? 'Order served'); invalidate(qc); }, onError: e => toast.error(msg(e, 'Failed to serve order')) }); }
export function useRequestCancelOrderMutation() { const qc = useQueryClient(); return useMutation({ mutationFn: ({ id, reason }: { id: string|number; reason?: string }) => orderService.requestCancel(id, reason), onSuccess: r => { toast.success(r.message ?? 'Cancel requested'); invalidate(qc); }, onError: e => toast.error(msg(e, 'Failed to request cancellation')) }); }
export function useCreateCreditAccountMutation(done?: () => void) { const qc = useQueryClient(); return useMutation({ mutationFn: (payload: CreditAccountPayload) => orderService.createCreditAccount(payload), onSuccess: r => { toast.success(r.message ?? 'Credit account saved'); invalidate(qc); done?.(); }, onError: e => toast.error(msg(e, 'Failed to save credit account')) }); }
export function useUpdateCreditAccountMutation(done?: () => void) { const qc = useQueryClient(); return useMutation({ mutationFn: ({ id, payload }: { id: string|number; payload: CreditAccountPayload }) => orderService.updateCreditAccount(id, payload), onSuccess: r => { toast.success(r.message ?? 'Credit account updated'); invalidate(qc); done?.(); }, onError: e => toast.error(msg(e, 'Failed to update credit account')) }); }
export function useToggleCreditAccountMutation() { const qc = useQueryClient(); return useMutation({ mutationFn: (id: string|number) => orderService.toggleCreditAccount(id), onSuccess: r => { toast.success(r.message ?? 'Credit account status updated'); invalidate(qc); }, onError: e => toast.error(msg(e, 'Failed to update credit account status')) }); }
export function useApproveCreditOrderMutation() { const qc = useQueryClient(); return useMutation({ mutationFn: (id: string|number) => orderService.approveCreditOrder(id), onSuccess: r => { toast.success(r.message ?? 'Credit order approved'); invalidate(qc); }, onError: e => toast.error(msg(e, 'Failed to approve credit order')) }); }
export function useSettleCreditOrderMutation(done?: () => void) { const qc = useQueryClient(); return useMutation({ mutationFn: ({ id, payload }: { id: string|number; payload: CreditSettlementPayload }) => orderService.settleCreditOrder(id, payload), onSuccess: r => { toast.success(r.message ?? 'Settlement saved'); invalidate(qc); done?.(); }, onError: e => toast.error(msg(e, 'Failed to settle credit order')) }); }
export function useCreatePackageMutation(done?: () => void) { const qc = useQueryClient(); return useMutation({ mutationFn: (payload: PackagePayload) => orderService.createPackage(payload), onSuccess: r => { toast.success(r.message ?? 'Package saved'); invalidate(qc); done?.(); }, onError: e => toast.error(msg(e, 'Failed to save package')) }); }
export function useDeletePackageMutation() { const qc = useQueryClient(); return useMutation({ mutationFn: (id: string|number) => orderService.deletePackage(id), onSuccess: r => { toast.success(r.message ?? 'Package deleted'); invalidate(qc); }, onError: e => toast.error(msg(e, 'Failed to delete package')) }); }
export function useCreatePackageOrderMutation(done?: () => void) { const qc = useQueryClient(); return useMutation({ mutationFn: (payload: PackageOrderPayload) => orderService.createPackageOrder(payload), onSuccess: r => { toast.success(r.message ?? 'Package order created'); invalidate(qc); done?.(); }, onError: e => toast.error(msg(e, 'Failed to create package order')) }); }
export function useSchedulePackageOrderMutation(done?: () => void) { const qc = useQueryClient(); return useMutation({ mutationFn: ({ id, payload }: { id: string|number; payload: PackageOrderSchedulePayload }) => orderService.schedulePackageOrder(id, payload), onSuccess: r => { toast.success(r.message ?? 'Schedule saved'); invalidate(qc); done?.(); }, onError: e => toast.error(msg(e, 'Failed to schedule package order')) }); }
export function usePackageOrderActionMutation() { const qc = useQueryClient(); return useMutation({ mutationFn: ({ id, action }: { id: string|number; action: 'approve'|'start-preparation'|'mark-ready'|'deliver'|'complete'|'cancel' }) => orderService.packageOrderAction(id, action), onSuccess: r => { toast.success(r.message ?? 'Package order updated'); invalidate(qc); }, onError: e => toast.error(msg(e, 'Failed to update package order')) }); }

export function useRecordBillPaymentMutation(done?: () => void) { const qc = useQueryClient(); return useMutation({ mutationFn: ({ billId, payload }: { billId: string|number; payload: PaymentPayload }) => orderService.recordBillPayment(billId, payload), onSuccess: r => { toast.success(r.message ?? 'Payment recorded'); invalidate(qc); done?.(); }, onError: e => toast.error(msg(e, 'Failed to record payment')) }); }
export function useConvertBillToCreditMutation(done?: () => void) { const qc = useQueryClient(); return useMutation({ mutationFn: ({ billId, payload }: { billId: string|number; payload: ConvertCreditPayload }) => orderService.convertBillToCredit(billId, payload), onSuccess: r => { toast.success(r.message ?? 'Bill converted to credit'); invalidate(qc); done?.(); }, onError: e => toast.error(msg(e, 'Failed to convert bill to credit')) }); }
export function useRejectCreditOrderMutation() { const qc = useQueryClient(); return useMutation({ mutationFn: ({ id, note }: { id: string|number; note?: string }) => orderService.rejectCreditOrder(id, note), onSuccess: r => { toast.success(r.message ?? 'Credit order rejected'); invalidate(qc); }, onError: e => toast.error(msg(e, 'Failed to reject credit order')) }); }
export function usePrepTicketActionMutation() { const qc = useQueryClient(); return useMutation({ mutationFn: ({ kind, id, action }: { kind: 'kitchen'|'bar'; id: string|number; action: 'accept'|'ready'|'served'|'reject'|'delay' }) => orderService.prepTicketAction(kind, id, action), onSuccess: r => { toast.success(r.message ?? 'Ticket updated'); qc.invalidateQueries({ queryKey: queryKeys.prepTickets.root() }); invalidate(qc); }, onError: e => toast.error(msg(e, 'Failed to update ticket')) }); }
